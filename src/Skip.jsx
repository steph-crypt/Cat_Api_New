import React from "react";

class Skip extends React.Component {
  constructor(props) {
    super(props);
  }
      render() {
        // this.props.handleGame();
      return(
        <button>Skip</button>
    );
  }
}

export default Skip;
